# Copy to .env.test. Overrides DATABASE_URL from .env so `npm test` runs
# against a disposable database instead of production — the suite deletes rows.
#
# Create one first:
#   createdb -h localhost -U postgres smashset_test
#   npm run migrate
DATABASE_URL=postgres://postgres:your-local-password@localhost:5432/smashset_test
